apiVersion: v1
kind: Service
metadata:
  name: simple-webapp-service
  labels:
    app: flask
spec:
  type: LoadBalancer
  loadBalancerIP: 35.207.142.40
  ports:
  - port: 7999
    targetPort: 8000
    protocol: TCP
  selector:
    app: flask
