apiVersion: v1
kind: Service
metadata:
  name: simple-webapp-service
  labels:
    app: flask
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    nodePort: 30007
  selector:
    app: flask